# requirements.txt inside your 'functions' directory
# This file specifies Python dependencies for your Cloud Function.

functions-framework==3.*
requests==2.*
google-generativeai==0.7.0 # Although we're using direct requests, including this might be useful for future direct client usage
Flask==3.* # flask is typically a dependency for functions-framework
